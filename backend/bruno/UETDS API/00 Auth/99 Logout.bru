meta {
  name: 99 Logout
  type: http
  seq: 99
}

post {
  url: {{baseUrl}}/auth/logout
  body: json
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "refresh": "{{refreshToken}}"
  }
}
